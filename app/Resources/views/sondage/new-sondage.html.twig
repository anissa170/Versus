{% extends 'layout.html.twig' %}

{% block body %}
    <section class="container">
        {{ form_start(form) }}
            {{ form_widget(form) }}
        <input type="submit">
        {{ form_end(form) }}
    </section>
{% endblock %}